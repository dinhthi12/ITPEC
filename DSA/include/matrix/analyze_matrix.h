#ifndef ANALYZE_MATRIX_H
#define ANALYZE_MATRIX_H

#include <vector>
#include <utility> // std::pair, std::make_pair

std::pair<int, int> analyzeMatrix(const std::vector<std::vector<int>> &matrix);

#endif